const AllocationResults = ({ results }) => {
    return (
      <div>
        <h2>Allocation Results</h2>
        <p>{results.message}</p>
      </div>
    );
  };
  
  export default AllocationResults;
  